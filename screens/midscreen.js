import {Text, View} from 'react-native';

const MidScreen = ({navigation}) => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Mid Screen</Text>
    </View>
  );
};
export default MidScreen;
